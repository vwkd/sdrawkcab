FunctionDeclaration
    "&" Block

Block
    "{" Module "}"

// note: allows any expression (e.g. variable declaration / assignment, if else, block, etc.), true first-class support, although not encouraged
FunctionCall
    "to" s Identifier
    "to" s FunctionDeclaration
    Expression s "to" s Identifier
    Expression s "to" s FunctionDeclaration