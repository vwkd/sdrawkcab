IfElse
    If
    If Else
    If ElseIf
    If ElseIf Else

// todo: don't allow any expression, e.g. variable declaration, assignment, return value, etc...?
If
    "if" s Expression sn Block

ElseIf
    "else" If
    "else" If ElseIf

Else
    "else" sn Block